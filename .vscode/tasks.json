{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Install new dependency and build static site",
			"type": "shell",
			"command": "npm ci || npm install && npm run build",
			"problemMatcher": [
				"$eslint-stylish"
			],
			"group": "build"
		},
		{
			"label": "Install deps (pnpm) and start dev",
			"type": "shell",
			"command": "pnpm install && pnpm run prepare && pnpm run dev",
			"isBackground": true,
			"group": "build"
		},
		{
			"label": "Install deps with npm fallback and run dev",
			"type": "shell",
			"command": "npm install && npm run prepare && npm run dev",
			"isBackground": true,
			"group": "build"
		}
	]
}