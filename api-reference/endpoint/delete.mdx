---
title: 'Delete Plant'
description: 'Remove a plant from the system'
---

<div style={{ display: 'flex', alignItems: 'center', gap: '16px', marginBottom: '24px' }}>
  <div>
    <Badge variant="danger">DELETE</Badge> 
    <code style={{ marginLeft: '8px', padding: '8px 12px', background: '#f1f5f9', borderRadius: '6px', fontWeight: '600' }}>/plants/&#123;id&#125;</code>
  </div>
  <button className="try-button" style={{ 
    background: '#16A34A', 
    color: 'white', 
    border: 'none', 
    padding: '8px 16px', 
    borderRadius: '8px', 
    fontWeight: '600', 
    cursor: 'pointer',
    fontSize: '14px'
  }}>
    Try it →
  </button>
</div>

Deletes a plant from the system by ID. This action cannot be undone.

## Authorization

<ParamField header="Authorization" type="string" required>
  Bearer authentication header of the form `Bearer <token>`, where `<token>` is your auth token.
</ParamField>

## Path Parameters

<ParamField path="id" type="integer" required>
  ID of plant to delete
</ParamField>

## Response

Returns `204 No Content` on successful deletion.

<RequestExample>

```bash cURL
curl --request DELETE \
  --url http://api.weiyuai.cn/plants/123 \
  --header 'Authorization: Bearer <token>'
```

</RequestExample>

<ResponseExample>

```json 204
No Content
```

```json 400
{
  "error": 400,
  "message": "Plant not found"
}
```

</ResponseExample>
